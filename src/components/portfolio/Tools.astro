---
import Capsule from "../ui/Capsule.astro";

type ToolVariant = "default" | "center";

const allTools = [
  {
    name: "🧁 Nothing Bundt Cakes 📍Camp Creek",
    link: "https://www.tiktok.com/t/ZTHEnXdvbfQT8-80WiK",
  },
  {
    name: "🍗 Wingstop 📍Camp Creek",
    link: "https://www.tiktok.com/t/ZTHEn4YmdWwGL-hLBsN",
  },
  {
    name: "🍤 Milk & Honey 📍 Cascade",
    link: "https://www.tiktok.com/t/ZTHEnVFS2V92p-iNiX5",
  },
  {
    name: "🍳 Breakfast at Barney's 📍 Downtown Atlanta",
    link: "https://www.tiktok.com/t/ZTHEnVSd1djmP-99ZBY",
  },
  {
    name: "🗣️ Shawn Food Reviews",
    link: "https://www.tiktok.com/t/ZTHEnV4sDfpD8-1wRPH",
  },
];

const baseClasses = "flex flex-wrap gap-4 max-lg:gap-1";
const variantClasses: Record<ToolVariant, string> = {
  default: "cursor-default",
  center: "justify-center cursor-default",
};

const {
  variant = "default",
  linkEnabled = true,
  size = "md",
} = Astro.props;

const classes = `${baseClasses} ${variantClasses[variant]}`;
---

<div class={classes}>
  {allTools.map((tool) => (
    <a
      href={tool.link}
      target="_blank"
      rel="noopener noreferrer"
      aria-label={tool.name}
    >
      <Capsule
        lang={tool.name}
        linkEnabled={linkEnabled}
        size={size}
      />
    </a>
  ))}
</div>